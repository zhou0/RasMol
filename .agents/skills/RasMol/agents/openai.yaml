interface:
  display_name: "RasMol"
  short_description: "Repo-specific patterns and workflows for RasMol"
  default_prompt: "Use the RasMol repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true