#include <stdio.h>
#include <stdint.h>
#include "src/rasmol.h"
int main() {
    printf("sizeof(Pixel): %zu\n", sizeof(Pixel));
    printf("sizeof(Long): %zu\n", sizeof(Long));
    printf("sizeof(int): %zu\n", sizeof(int));
    return 0;
}
